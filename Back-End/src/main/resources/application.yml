jwt:
  secret: ${JWT_SECRET:MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI=}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

# CORS 허용 도메인 (쉼표로 구분)
# 예: cors.allowed-origins=http://localhost:5173,https://your-app.vercel.app
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dongseo?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 5776
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
        dialect: org.hibernate.dialect.MySQLDialect
        physical_naming_strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

  sql:
    init:
      mode: always
      schema-locations: classpath:fix_schema.sql
      data-locations: classpath:data.sql
      separator: ;
      continue-on-error: true